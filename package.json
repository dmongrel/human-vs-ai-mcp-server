{
  "name": "human-vs-ai-mcp-server",
  "version": "0.0.2",
  "description": "MCP server for detecting AI-generated text and offering humanizing recommendations.",
  "main": "dist/index.js",
  "bin": {
    "human-vs-ai-mcp-server": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "build:native": "powershell -ExecutionPolicy Bypass -File native/llama-engine/build-win32-x64.ps1",
    "start": "node dist/index.js",
    "pretest": "tsc -p tsconfig.test.json",
    "test": "node --test \"dist-test/**/*.test.js\"",
    "prepublishOnly": "npm run build",
    "check:native": "node scripts/check-native-package.js",
    "release": "npm run build && npm run build:native && npm run check:native && npm publish ./packages/win32-x64 && npm publish",
    "release:dry": "npm run build && npm run check:native && npm publish ./packages/win32-x64 --dry-run && npm publish --dry-run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "optionalDependencies": {
    "human-vs-ai-mcp-server-win32-x64": "0.0.2"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.5.3"
  },
  "private": false
}
